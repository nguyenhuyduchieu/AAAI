.DS_Store
.cache/
__pycache__/
*.pyc

# avoid committing nested cloned repositories as broken gitlinks
external/

# --- Chỉ loại dataset trong repo (thư mục gốc data/) ---
/data/

# Python / Conda environments — never commit (large binaries, local paths).
.venv/
venv/
.venv_*/
/conda_*/
.env/

# Notebook dataset mirrors (very large CSV/zip; not source code)
notebooks/data/

# LaTeX auxiliary at repo root
/main.aux
/main.log
/texput.log
